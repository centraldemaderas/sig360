kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # App Hosting buildpacks standard installation
  buildCommand: npm install --no-audit --no-fund && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024