kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # Comando optimizado: garantiza que el entorno de build sea idéntico al de desarrollo
  buildCommand: npm ci && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024