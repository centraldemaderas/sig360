kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # Usamos npm ci para garantizar que el build coincide exactamente con el lockfile
  buildCommand: npm ci && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024