kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # Comando inmutable: ignora la caché corrupta y verifica integridad total
  buildCommand: npm ci --no-audit --no-fund && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024