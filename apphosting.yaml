kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # npm ci garantiza que el despliegue sea 100% reproducible y coincida con el lockfile
  buildCommand: npm ci && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024