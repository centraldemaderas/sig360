kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # Comando optimizado para Cloud Build: garantiza sincronización total con el lockfile
  buildCommand: npm ci && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024