kind: AppStack
schemaVersion: v1
buildConfig:
  runtime: nodejs20
  # Usamos npm install para que sea menos estricto con el lockfile manual
  buildCommand: npm install && npm run build
runConfig:
  runCommand: npm start
  cpu: 1
  memoryMiB: 1024