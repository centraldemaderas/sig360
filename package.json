{
  "name": "sig-manager-pro",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p dist && npx esbuild index.tsx --bundle --outfile=dist/index.js --format=esm --jsx=automatic --minify --external:react --external:react-dom/* --external:lucide-react --external:recharts --external:firebase/* && cp index.html metadata.json dist/",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0",
    "jsdom": "^24.0.0"
  }
}
