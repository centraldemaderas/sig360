{
  "name": "sig360",
  "version": "1.2.7",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npx serve .",
    "build": "esbuild index.tsx --bundle --outdir=dist --format=esm --jsx=automatic --minify --external:react --external:react-dom/* --external:lucide-react --external:recharts --external:firebase/* && cp index.html metadata.json dist/ && node --input-type=module -e \"import fs from 'node:fs'; const path='dist/index.html'; const h=fs.readFileSync(path,'utf8'); fs.writeFileSync(path, h.replace('index.tsx','index.js'))\"",
    "start": "node server.js"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.460.0",
    "recharts": "^2.13.0",
    "firebase": "^11.0.2",
    "express": "^4.18.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "xlsx": "^0.18.5",
    "pdfjs-dist": "^4.8.62"
  }
}